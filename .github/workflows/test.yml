name: "test"

on: [push, pull_request]

jobs:
  test:
    name: "Unit tests"
    runs-on: "ubuntu-latest"

    steps:
      - name: git-checkout
        uses: actions/checkout@v2

      - name: Install all dependencies
        run: npm install

      - name: Test
        run: npm test
